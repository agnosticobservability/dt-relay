# Default environment values for dt-relay
# Copy to a secure location or override via docker-compose or runtime environment
# Leaving AUTH_PASSWORD empty allows the server to start but all submissions will be rejected until it is set.
AUTH_PASSWORD=changeme
DEFAULT_DIM_HOST=dd-system-01
DEFAULT_DIM_ENVIRONMENT=primary-dc
METRIC_PREFIX=custom.ddfs
# Optional labels exposed on the /metrics/ endpoint (JSON or comma-separated key=value pairs)
# METRICS_CUSTOM_LABELS=
