# Default environment values for dt-relay
# Copy to a secure location or override via docker-compose or runtime environment
AUTH_PASSWORD=changeme
DEFAULT_DIM_SYSTEM=dd-system-01
DEFAULT_DIM_SITE=primary-dc
METRIC_PREFIX=custom.ddfs
